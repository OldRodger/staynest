services:
  mongodb:
    image: mongo
    restart: always
    ports:
      - "27016:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example

  backend:
    depends_on:
      - mongodb
    build: .
    image: stay_nest
    ports:
      - "80:80"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - ./.env


